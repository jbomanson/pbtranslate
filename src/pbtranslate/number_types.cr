module PBTranslate
  # A type used for network depths, wire numbers and small wire counts.
  alias Distance = UInt32

  # A type used for potentially big network related numbers such as network
  # read and write counts.
  alias Area = UInt64
end
